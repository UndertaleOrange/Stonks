// Computer identification (Desktop, Laptop, Workstation)

{
    return 
        os_type == os_windows or
        os_type == os_win8native or
        os_type == os_macosx or
        os_type == os_linux;
}
